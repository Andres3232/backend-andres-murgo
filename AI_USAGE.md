# AI Usage

## Herramienta utilizada

**Google Gemini**

---

## Para qué se usó

- **Lógica del servicio de dominio**: se usó Gemini para asistir en la implementación de la lógica de los dos casos de uso principales (`getActiveLeafPaths` y `findCategoryById`) dentro de `category-tree.service.ts`.
- **Tests**: generación de los archivos de test para ambas fases (`get-active-leaf-paths.spec.ts` y `find-category-by-id.spec.ts`).
- **Documentación**: generación del `README.md` y de este archivo `AI_USAGE.md`.
